spring.jpa.database=POSTGRESQL
spring.sql.init.platform=postgres
spring.datasource.url=jdbc:postgresql://localhost:5433/postgres
spring.datasource.username=postgres
spring.datasource.password=4208
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true


spring.h2.console.enabled=true
# default path: h2-console
spring.h2.console.path=/h2-ui
 
spring.datasource.driverClassName=org.h2.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# App Properties
bezkoder.app.jwtCookieName=bezkoder
bezkoder.app.jwtSecret= ======================BezKoder=Spring===========================
bezkoder.app.jwtExpirationMs=86400000
# Configuración de JWT

# Configuración de JWT
jwt.secret=TuClaveSecreta  # Clave secreta para firmar y verificar tokens
jwt.expirationTime=86400000  # Tiempo de expiración del token en milisegundos (1 día en este ejemplo)
jwt.tokenPrefix=Bearer  # Prefijo del token JWT en las solicitudes
jwt.headerString=Authorization  # Nombre del encabezado que contiene el token JWT

